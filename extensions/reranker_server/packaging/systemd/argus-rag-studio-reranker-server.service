# Argus RAG Studio Reranker Server — 패키지 설치용(install.sh 가 __INSTALL_DIR__ 치환)
# 수동(systemd) 배포는 에이전트 탐색 대상이 아니다 — 설정 URL 프로브/heartbeat 로 관측
# (deploy/reranker-server-deployment.md 의 ARGUS_HEARTBEAT_URL 참조).
[Unit]
Description=Argus RAG Studio Reranker Server
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
WorkingDirectory=__INSTALL_DIR__
Environment=ARGUS_RERANKER_SERVER_CONFIG_DIR=__INSTALL_DIR__/config
ExecStart=__INSTALL_DIR__/venv/bin/python -m reranker_server
Restart=on-failure
RestartSec=5
LimitNOFILE=65536
StandardOutput=journal
StandardError=journal
SyslogIdentifier=argus-rag-studio-reranker-server

[Install]
WantedBy=multi-user.target
