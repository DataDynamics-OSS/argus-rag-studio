fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0
PyYAML>=6.0
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30.0
aiomysql>=0.2.0
email-validator>=2.0
python-jose[cryptography]>=3.3.0
httpx>=0.27
cachetools>=5.3
python-multipart>=0.0.9
boto3>=1.34
pgvector>=0.2.5
pypdf>=4.0
python-docx>=1.1
openpyxl>=3.1
python-pptx>=0.6
mammoth>=1.6
pyarrow>=14.0
olefile>=0.47
anthropic>=0.40

# 로컬(자체) 임베딩 — FastEmbed(ONNX, torch 불필요)
fastembed>=0.4

# 토큰 기반 청킹 사이징(chunk_unit=token). 미설치 시 char 폴백.
tiktoken>=0.7

# 한국어 문장 분리(chunk_strategy=sentence). 약어·소수점·종결어미 정확.
#   미설치 시 규칙 기반(약어/소수점 보호) 폴백. mecab 없이 pecab 백엔드로 동작.
kss>=4.5

# 파싱 전략(선택) — parse_strategy 로 컬렉션별 선택. 미설치 시 해당 전략만 비활성.
#   layout: PDF 표를 Markdown 으로 직렬화(가성비 높은 다음 단계)
pdfplumber>=0.11
#   docai: 문서 AI(레이아웃·표·읽기순서). torch/rapidocr 등 수 GB — 설치 용량 큼.
docling>=2.0
#   vlm: 페이지 이미지 렌더링(비전 LLM 파서). 비전 모델 엔드포인트 필요.
#   어노테이션 문서→이미지 변환(PDF/오피스 페이지 래스터화)에도 사용.
pymupdf>=1.24

# 워커 리소스 메트릭(프로세스·호스트 CPU/RAM) 수집
psutil>=5.9
# 워커 호스트 GPU 메트릭(NVML) — GPU/드라이버 없으면 자동 비활성(import 만 안전)
nvidia-ml-py>=12.0
